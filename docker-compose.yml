version: '3.8'

services:
  document-qa:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    env_file:
      - .env
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped
